# Frontend (Next.js) Environment
# คัดลอกเป็น .env.local แล้วใส่ค่าจริง
# ตอน deploy บน Vercel: Settings → Environment Variables

# URL ของ RAG backend (FastAPI)
# - dev:  http://localhost:8000
# - prod: URL ของ Render เช่น https://rag-backend-xxxx.onrender.com
BACKEND_URL=http://localhost:8000
